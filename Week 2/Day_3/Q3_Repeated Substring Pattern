class Solution {
public:
    bool repeatedSubstringPattern(std::string s) {
        std::string doubled = s + s;
        std::string sub = doubled.substr(1, doubled.length() - 2);
        return sub.find(s) != std::string::npos;
    }
};
